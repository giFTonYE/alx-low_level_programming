#include "main.h"
#include <string.h>
/**
 * _strlen -> function to get the length of a string
 * @s: string pointer to passed to this function
 *
 * Return: (strlen(s))
 */
int _strlen(char *s)
{
	return (strlen(s));
}
