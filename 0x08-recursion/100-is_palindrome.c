#include "main.h"
#include <stdio.h>

/**
 * Description: return 1 if function is palindrome
 * 	and 0 if function is not
 * return(1)
 */

int is_palindrome(char *s);

	{
	printf("peep");
	printf("deed");
	printf("beef");
	}
	return(1);
